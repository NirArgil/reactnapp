import React from 'react'
import { StyleSheet, View, FlatList, Image } from 'react-native';
import { Text } from 'react-native-elements';

const ShirtsScreen = () => {
    return (
        <View>
           <Text h1>Shirts Screen</Text> 
        </View>
    )
}

const styles = StyleSheet.create({
  
});

export default ShirtsScreen;